# Env files — NEVER commit these
.env
.env.local

# Foundry build artifacts
contracts/out/
contracts/cache/
contracts/lib/
contracts/broadcast/

# Claude Code — entire dir is personal config (worktrees, commands, hooks,
# settings). MCP server wiring + the project-context file are personal too.
.claude/
.hermes/
.mcp.json
CLAUDE.md

# Node
scripts/node_modules/
scripts/dist/

# System
.DS_Store
.vercel/
contracts/lib/
