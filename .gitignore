# Env files — NEVER commit these
.env
.env.local

# Foundry build artifacts
contracts/out/
contracts/cache/
contracts/lib/
contracts/broadcast/

# Claude Code — entire dir is personal config (worktrees, commands, hooks,
# settings). MCP server wiring is also user-specific.
.claude/
.mcp.json

# Node
scripts/node_modules/
scripts/dist/

# System
.DS_Store
.vercel/
contracts/lib/
