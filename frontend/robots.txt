User-agent: *
Allow: /

# Video-recording demo. Everything behind this path is fabricated data used
# for screen recordings — it must never be indexed as if it were real
# Pronos market activity.
Disallow: /points/video

# Presentation demo. Real market questions, but every price, trade and volume
# behind this path is simulated in the browser — it must never be indexed as
# if it were real Pronos activity.
Disallow: /points-demo
